
struct Ray {
    vec3 origin;
    vec3 direction;
};

Ray getter() {
    Ray ray = Ray(vec3(1.0, 2.0, 3.0), vec3(0.0, 1.0, 0.0));

    ray.direction = vec3(8.0, 1.0, 7.0);

    return ray;
}

export int main() {

    Ray ray = getter();

    vec3 origin = vec3(7.0, 8.0, 9.0);

    ray = Ray(vec3(7.0, 8.0, 9.0), vec3(7.0, 8.0, 9.0));

    int i = 0;
    for (i = 0; i < 10; i+= 1) {
    }

    return i;
}
