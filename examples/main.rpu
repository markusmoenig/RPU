//fun fib(n) {
//  if (n <= 1) return n;
//  return fib(n - 2) + fib(n - 1);
//}

int doubler(int y) {
    return y * 2;
}

export int main(int x) {
    ivec2 result = 2 * ivec2(5, 2);

    return doubler(result.x);

    //return result.xy;
}
